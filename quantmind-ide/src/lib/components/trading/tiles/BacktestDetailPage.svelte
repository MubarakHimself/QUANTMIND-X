<script lang="ts">
  /**
   * BacktestDetailPage — placeholder for backtest detail view.
   * Full content supplied by Epic 8.
   * AC 12-4-7: sub-page routing placeholder
   * Story 12-4
   */
</script>

<div class="detail-page">
  <h2 class="page-title">Backtest Detail</h2>
  <p class="page-note">Full backtest viewer — Epic 8</p>
</div>

<style>
  .detail-page {
    padding: var(--space-6, 1.5rem) var(--space-4);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .page-title {
    font-family: var(--font-heading);
    font-size: var(--text-xl);
    color: var(--color-text-primary);
    margin: 0;
  }

  .page-note {
    font-family: var(--font-ambient);
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
</style>
